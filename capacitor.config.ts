import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
    appId: "nz.co.bosher.stuff-test",
    appName: "stuff-test",
    webDir: "build",
    bundledWebRuntime: false
};

export default config;
